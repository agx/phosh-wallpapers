theme = files(
  'message-new-email.oga',
  'message-new-instant.oga',
  'phone-incoming-call.oga',
  'system-ready.oga',
  'video-incoming-call.oga',
)

install_data(theme, install_dir: sounddir / 'stereo')
